<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.anad19ps.student.turtle">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Turtle">
        <activity android:name=".CreateCustomCommandActivity"></activity>
        <activity android:name=".ManageCustomCommandsActivity" />
        <activity
            android:name=".ControllerActivity"
            android:label="@string/title_activity_controller"
            android:theme="@style/Theme.Turtle.NoActionBar" />
        <activity
            android:name=".MainActivity2"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.Turtle.NoActionBar" />
        <activity
            android:name=".SelectBluetoothDeviceActivity"
            android:label="@string/title_activity_bluetooth"
            android:theme="@style/Theme.Turtle.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>